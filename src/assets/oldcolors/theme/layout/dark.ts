import {palette} from '../../palette/palette';

const dark = {
  background: palette.gray900,
  surface: palette.gray800,
  divider: palette.gray700,
  sport: palette.glow600,
  disabled: palette.gray700,
  navbar: palette.gray800,
};

export default dark;
