import {palette} from '../../palette/palette';

const light = {
  background: palette.white,
  surface: palette.gray30,
  divider: palette.gray100,
  sport: palette.glow600,
  disabled: palette.gray100,
  navbar: palette.gray30,
};

export default light;
