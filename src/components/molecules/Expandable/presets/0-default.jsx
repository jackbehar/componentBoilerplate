import Expandable from "../index";

export default (
  <Expandable
    uxpId="expandble-1"
    defaultExpanded={false}
    forceExpand={undefined}
    label="not expanded"
    labelExpanded="expanded"
  />
);
