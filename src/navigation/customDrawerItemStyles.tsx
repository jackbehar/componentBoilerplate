import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  icon: {
    alignSelf: 'flex-end',
  },
  label: {
    color: '#000',
    marginLeft: 0,
    paddingLeft: 0,
  },
});

export default styles;
